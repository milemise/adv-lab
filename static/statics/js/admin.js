let adminToken='';
let adminArticles=[];
const A=id=>document.getElementById(id);
const errorText=data=>{if(data?.detail){if(typeof data.detail==='string')return data.detail;if(Array.isArray(data.detail))return data.detail.map(item=>item?.msg||item?.detail||JSON.stringify(item)).join(' · ');return JSON.stringify(data.detail)}return 'Solicitud fallida'};
const adminFetch=async(url,options={})=>{const headers={...(options.headers||{}),'X-Admin-Token':adminToken};const response=await fetch(url,{...options,headers});const data=await response.json().catch(()=>({}));if(!response.ok)throw new Error(errorText(data));return data};
const adminToast=message=>{const el=A('adminToast');el.textContent=message;el.classList.add('show');clearTimeout(window.__adminToast);window.__adminToast=setTimeout(()=>el.classList.remove('show'),3200)};
const esc=t=>{const d=document.createElement('div');d.textContent=t??'';return d.innerHTML};
function categoryOptions(list,current=''){return `<option value="">Seleccionar categoría</option>${list.map(x=>`<option value="${x.id}" ${String(x.id)===String(current)?'selected':''}>${esc(x.icon||'✦')} ${esc(x.name)}</option>`).join('')}`}
async function bootAdmin(){try{const search=(A('requestSearch')?.value||'').trim();const [stats,pending,products,requests,categories,articles]=await Promise.all([adminFetch('/api/admin/stats'),adminFetch('/api/admin/pending'),adminFetch('/api/admin/products'),adminFetch(`/api/admin/contact-requests?search=${encodeURIComponent(search)}`),fetch('/api/categories').then(r=>r.json()),adminFetch('/api/admin/articles')]);A('dashPending').textContent=stats.pending;A('dashProducts').textContent=stats.active_products;A('dashRequests').textContent=stats.requests;A('dashPhotos').textContent=stats.photos;A('productCategory').innerHTML=categoryOptions(categories);adminArticles=articles;renderPending(pending);renderProducts(products);renderRequests(requests);renderArticles(articles)}catch(error){A('loginError').textContent=error.message;A('loginError').classList.remove('hidden')}}
function renderPending(items){A('pendingList').innerHTML=items.length?items.map(p=>`<article class="panel-card rounded-2xl overflow-hidden"><img src="${esc(p.image)}" class="w-full aspect-[16/10] object-cover" alt=""><div class="p-4"><div class="flex justify-between gap-2"><div><h3 class="font-display text-xl">${esc(p.title)}</h3><p class="text-xs text-slate-500 mt-1">${esc(p.photographer||'Comunidad')} · ${esc(p.instagram||'sin Instagram')} · ${esc(p.location||'sin ubicación')}</p></div><span class="photo-tag">${esc(p.category||'Otros')}</span></div><div class="flex gap-2 mt-4"><button class="primary-btn !min-h-0" type="button" data-moderate="approve" data-id="${p.id}">Aprobar</button><button class="secondary-btn !min-h-0" type="button" data-moderate="reject" data-id="${p.id}">Descartar</button></div></div></article>`).join(''):`<div class="panel-card rounded-2xl p-8 text-slate-500">No hay aportes pendientes.</div>`}
function renderProducts(items){A('productsAdmin').innerHTML=items.map(p=>`<article class="panel-card rounded-2xl p-4 flex gap-4"><img src="${esc(p.image||'https://images.unsplash.com/photo-1462331940025-496dfbfc7564?auto=format&fit=crop&w=500&q=80')}" class="w-28 h-24 object-cover rounded-xl" alt=""><div class="min-w-0 flex-1"><span class="product-category">${esc(p.category)}</span><h3 class="font-display text-xl">${esc(p.name)}</h3><p class="text-xs text-slate-500">$ ${Number(p.price).toLocaleString('es-AR')} · stock ${p.stock} · ${p.active?'visible':'oculto'}</p><div class="flex gap-2 mt-3"><button class="secondary-btn !min-h-0 !py-2 !px-3 text-xs" type="button" data-edit-product="${p.id}">Editar</button>${p.active?`<button class="secondary-btn !min-h-0 !py-2 !px-3 text-xs" type="button" data-product-state="hide" data-id="${p.id}">Ocultar</button>`:`<button class="secondary-btn !min-h-0 !py-2 !px-3 text-xs" type="button" data-product-state="restore" data-id="${p.id}">Restaurar</button>`}</div></div></article>`).join('')}
function renderRequests(items){A('requestsList').innerHTML=items.length?items.map(r=>`<article class="panel-card rounded-2xl p-4"><div class="flex flex-col md:flex-row justify-between gap-3"><div><span class="product-category">${esc(r.product_name||'Consulta general')}</span><h3 class="font-display text-xl">${esc(r.full_name)}</h3><p class="text-sm text-slate-400 mt-1">WhatsApp: ${esc(r.whatsapp)} · ${esc(r.email)}</p><p class="text-sm text-slate-300 mt-3 leading-6">${esc(r.message)}</p></div><select class="select-input !w-auto" data-request-status="${r.id}"><option value="nuevo" ${r.status==='nuevo'?'selected':''}>nuevo</option><option value="contactado" ${r.status==='contactado'?'selected':''}>contactado</option><option value="cerrado" ${r.status==='cerrado'?'selected':''}>cerrado</option></select></div></article>`).join(''):`<div class="panel-card rounded-2xl p-8 text-slate-500">No hay consultas que coincidan.</div>`}
function renderArticles(items){A('articlesAdmin').innerHTML=items.length?items.map(a=>`<article class="panel-card rounded-2xl p-4"><div class="flex flex-col md:flex-row gap-4 justify-between"><div class="min-w-0"><div class="flex flex-wrap gap-2 mb-2"><span class="source-chip">${a.published?'PUBLICADA':'OCULTA'}</span><span class="product-category">/${esc(a.slug)}</span></div><h3 class="font-display text-2xl">${esc(a.title)}</h3><p class="text-sm text-slate-400 mt-2">${escapeSummary(a.content)}</p></div><div class="flex gap-2 shrink-0"><button class="secondary-btn !min-h-0 !py-2 !px-3 text-xs" type="button" data-edit-article="${a.id}">Editar</button>${a.published?`<button class="secondary-btn !min-h-0 !py-2 !px-3 text-xs" type="button" data-delete-article="${a.id}">Ocultar</button>`:`<button class="secondary-btn !min-h-0 !py-2 !px-3 text-xs" type="button" data-restore-article="${a.id}">Publicar</button>`}</div></div></article>`).join(''):`<div class="panel-card rounded-2xl p-8 text-slate-500">No hay notas cargadas.</div>`}
function escapeSummary(text){const clean=esc(String(text||''));return clean.length>260?clean.slice(0,260)+'…':clean}
async function moderate(id,action){try{await adminFetch(action==='approve'?`/api/admin/approve/${id}`:`/api/admin/reject/${id}`,{method:action==='approve'?'POST':'DELETE'});adminToast(action==='approve'?'Aporte aprobado':'Aporte descartado y conservado en la base de datos');bootAdmin()}catch(e){adminToast(e.message)}}
async function productState(id,state){try{await adminFetch(state==='hide'?`/api/admin/products/${id}`:`/api/admin/products/${id}/restore`,{method:state==='hide'?'DELETE':'POST'});adminToast(state==='hide'?'Producto oculto':'Producto restaurado');bootAdmin()}catch(e){adminToast(e.message)}}
function editProduct(id){adminFetch('/api/admin/products').then(items=>{const p=items.find(x=>x.id===id);if(!p)return;A('productId').value=p.id;A('productName').value=p.name;A('productDescription').value=p.description||'';A('productPrice').value=p.price||0;A('productStock').value=p.stock||0;A('productImage').value=p.image||'';A('productContact').value=p.contact_url||'';A('productCategory').value=p.category_id||'';document.getElementById('tab-productos').scrollIntoView({behavior:'smooth',block:'start'})}).catch(e=>adminToast(e.message))}
function editArticle(id){const a=adminArticles.find(x=>x.id===id);if(!a)return;A('articleId').value=a.id;A('articleFormTitle').textContent='Editar nota';A('articleTitle').value=a.title;A('articleSlug').value=a.slug;A('articleContent').value=a.content;A('articleCover').value=a.cover||'';document.getElementById('tab-contenido').scrollIntoView({behavior:'smooth',block:'start'})}
function resetArticle(){A('articleForm').reset();A('articleId').value='';A('articleFormTitle').textContent='Agregar nota'}
async function articleState(id,action){try{if(action==='delete'){await adminFetch(`/api/admin/articles/${id}`,{method:'DELETE'});adminToast('Nota ocultada. Los datos se conservaron.')}else{const a=adminArticles.find(x=>x.id===id);if(!a)return;await adminFetch(`/api/admin/articles/${id}`,{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify({title:a.title,slug:a.slug,content:a.content,author:a.author,cover:a.cover,published:true})});adminToast('Nota publicada')}bootAdmin()}catch(e){adminToast(e.message)}}
A('adminToken')?.addEventListener('keydown',e=>{if(e.key==='Enter')A('loginBtn').click()});
document.addEventListener('click',async event=>{const tab=event.target.closest('.admin-tab');if(tab){document.querySelectorAll('.admin-tab').forEach(x=>x.classList.remove('active'));tab.classList.add('active');document.querySelectorAll('.admin-tab-panel').forEach(x=>x.classList.add('hidden'));A(`tab-${tab.dataset.tab}`).classList.remove('hidden');return}const moderateBtn=event.target.closest('[data-moderate]');if(moderateBtn){await moderate(Number(moderateBtn.dataset.id),moderateBtn.dataset.moderate);return}const stateBtn=event.target.closest('[data-product-state]');if(stateBtn){await productState(Number(stateBtn.dataset.id),stateBtn.dataset.productState);return}const editBtn=event.target.closest('[data-edit-product]');if(editBtn){editProduct(Number(editBtn.dataset.editProduct));return}const editArticleBtn=event.target.closest('[data-edit-article]');if(editArticleBtn){editArticle(Number(editArticleBtn.dataset.editArticle));return}const deleteArticleBtn=event.target.closest('[data-delete-article]');if(deleteArticleBtn){await articleState(Number(deleteArticleBtn.dataset.deleteArticle),'delete');return}const restoreArticleBtn=event.target.closest('[data-restore-article]');if(restoreArticleBtn){await articleState(Number(restoreArticleBtn.dataset.restoreArticle),'restore');return}});
A('loginBtn')?.addEventListener('click',async()=>{adminToken=A('adminToken').value.trim();try{await adminFetch('/api/admin/stats');A('loginBox').classList.add('hidden');A('adminApp').classList.remove('hidden');A('loginError').classList.add('hidden');bootAdmin()}catch(e){A('loginError').textContent='Token inválido o servidor no disponible';A('loginError').classList.remove('hidden')}});
A('productForm')?.addEventListener('submit',async e=>{e.preventDefault();const payload={name:A('productName').value,description:A('productDescription').value,price:Number(A('productPrice').value||0),image:A('productImage').value,stock:Number(A('productStock').value||0),active:true,contact_url:A('productContact').value||'https://ig.me/m/astrodevic',category_id:Number(A('productCategory').value)||null};try{const id=A('productId').value;await adminFetch(id?`/api/admin/products/${id}`:'/api/admin/products',{method:id?'PUT':'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)});A('productForm').reset();A('productId').value='';adminToast('Producto guardado');bootAdmin()}catch(error){adminToast(error.message)}});
A('resetProduct')?.addEventListener('click',()=>{A('productForm').reset();A('productId').value=''});
A('uploadAsset')?.addEventListener('click',async()=>{const file=A('assetFile').files?.[0];if(!file)return adminToast('Elegí una imagen');const form=new FormData();form.append('file',file);try{const result=await adminFetch('/api/admin/upload',{method:'POST',body:form});A('productImage').value=result.url;adminToast('Imagen subida')}catch(e){adminToast(e.message)}});
A('articleForm')?.addEventListener('submit',async e=>{e.preventDefault();const id=A('articleId').value;const payload={title:A('articleTitle').value,slug:A('articleSlug').value,content:A('articleContent').value,author:'ADV-Lab',cover:A('articleCover').value,published:true};try{await adminFetch(id?`/api/admin/articles/${id}`:'/api/admin/articles',{method:id?'PUT':'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)});resetArticle();adminToast(id?'Nota actualizada':'Nota publicada');bootAdmin()}catch(error){adminToast(error.message)}});
A('resetArticle')?.addEventListener('click',resetArticle);
A('requestSearch')?.addEventListener('keydown',e=>{if(e.key==='Enter'){e.preventDefault();bootAdmin()}});
A('requestSearch')?.addEventListener('input',()=>{clearTimeout(window.__requestSearchTimer);window.__requestSearchTimer=setTimeout(bootAdmin,350)});
document.addEventListener('change',async event=>{const select=event.target.closest('[data-request-status]');if(!select)return;try{await adminFetch(`/api/admin/contact-requests/${select.dataset.requestStatus}/status?status=${encodeURIComponent(select.value)}`,{method:'POST'});adminToast('Estado actualizado')}catch(error){adminToast(error.message)}});
